Drop your vector logo PDF here for pixel-perfect rendering.

Steps:
- Export the provided logo as a single-page PDF (vector), transparent background.
- Save it as `LangscapeBrandmark.pdf`.
- Put it inside:
  `UIComponents/Sources/UIComponents/Resources/BrandAssets.xcassets/LangscapeBrandmark.imageset/`

Xcode settings for the image set (already in Contents.json):
- Preserves Vector Representation: true
- Render As: Original Image (no template tinting)

After adding the file, clean build and run.
